import 'package:flutter/material.dart';

class AppTheme {
  static ThemeData lightTheme = ThemeData(
    // Colori personalizzati per il tema chiaro
    primaryColor: Colors.red, // Modifica il colore primario come preferisci
    hintColor: Colors.blue, // Modifica il colore suggerimenti come preferisci
    colorScheme: const ColorScheme.dark(), // Utilizza lo schema di colori predefinito per il tema chiaro
    // Altri stili e configurazioni del tema qui...
  );
}
